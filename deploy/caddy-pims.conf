:80 {
	# PIMS — strip /pims, proxy to backend on /
	handle_path /pims/* {
		reverse_proxy localhost:8083
	}

	# Landing page at root
	handle / {
		root * /var/www/landing
		file_server
	}

	# Procura — everything else
	handle {
		reverse_proxy localhost:8082
	}
}
